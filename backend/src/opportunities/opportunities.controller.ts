import { Body, Controller, Get, Param, ParseUUIDPipe, Post, Query, UseGuards } from '@nestjs/common'; import { Type } from 'class-transformer'; import { IsArray, IsDate, IsInt, IsNumber, IsOptional, IsString, IsUUID, Length, Min, MinLength, ValidateNested } from 'class-validator'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { AuthUser } from '../auth/jwt.strategy'; import { CurrentUser } from '../common/current-user.decorator'; import { PaginationQueryDto } from '../common/pagination.dto'; import { OpportunitiesService } from './opportunities.service';
class RequestDto { @IsString() @MinLength(3) title!:string; @IsString() @MinLength(5) details!:string; @IsString() category!:string; @IsOptional() @IsString() city?:string; @IsOptional() @IsString() province?:string; @IsOptional() @IsNumber() @Min(0) budget?:number; @IsOptional() @Length(3,3) currency?:string; @IsOptional() @IsInt() @Min(1) quantity?:number; @IsOptional() @Type(()=>Date) @IsDate() expiresAt?:Date; }
class QuoteLineDto { @IsOptional() @IsUUID() productId?:string; @IsString() description!:string; @IsInt() @Min(1) quantity!:number; @IsNumber() @Min(0) unitPrice!:number; }
class QuoteDto { @IsNumber() @Min(0) amount!:number; @Length(3,3) currency!:string; @IsOptional() @IsString() message?:string; @Type(()=>Date) @IsDate() validUntil!:Date; @IsOptional() @IsArray() @ValidateNested({each:true}) @Type(()=>QuoteLineDto) items?:QuoteLineDto[]; }
@Controller('requests') @UseGuards(JwtAuthGuard) export class OpportunitiesController { constructor(private readonly s:OpportunitiesService){} @Post() create(@CurrentUser()u:AuthUser,@Body()d:RequestDto){return this.s.createRequest(u.userId,u.accountType,d)} @Get('mine') mine(@CurrentUser()u:AuthUser){return this.s.mine(u.userId)} @Get('business/:businessId') relevant(@CurrentUser()u:AuthUser,@Param('businessId',ParseUUIDPipe)b:string,@Query()query:PaginationQueryDto){return this.s.relevant(u.userId,b,query)} @Post(':requestId/business/:businessId/quotes') quote(@CurrentUser()u:AuthUser,@Param('requestId',ParseUUIDPipe)r:string,@Param('businessId',ParseUUIDPipe)b:string,@Body()d:QuoteDto){return this.s.quote(u.userId,b,r,d)} @Post(':requestId/quotes/:quoteId/accept') accept(@CurrentUser()u:AuthUser,@Param('requestId',ParseUUIDPipe)r:string,@Param('quoteId',ParseUUIDPipe)q:string){return this.s.accept(u.userId,r,q)} }
