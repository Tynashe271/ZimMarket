import { Controller, Delete, Get, UseGuards } from '@nestjs/common'; import { JwtAuthGuard } from '../auth/jwt-auth.guard'; import { AuthUser } from '../auth/jwt.strategy'; import { CurrentUser } from '../common/current-user.decorator'; import { PrivacyService } from './privacy.service';
@Controller('privacy') @UseGuards(JwtAuthGuard) export class PrivacyController { constructor(private readonly s: PrivacyService) {} @Get('export') export(@CurrentUser() u: AuthUser) { return this.s.export(u.userId); } @Delete('account') remove(@CurrentUser() u: AuthUser) { return this.s.remove(u.userId); } }
