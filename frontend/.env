# Browser requests use /api/v1 and Next.js securely forwards them to NestJS.
BACKEND_URL=http://localhost:3007
# Optional public URL for deployments where the API is on another origin.
NEXT_PUBLIC_API_URL=
